import EmotionListItem from "./EmotionListItem";
 
import './EmoList.css';

function EmotionList () {
    return (
        <p>Emotions</p>
    );


    //<div className="">
    //{props.EmotionList.map( (el) => (<EmotionListItem emoitem={el} />) )}
    // </div>
}

export default EmotionList;